import { styled } from "styled-components";

export const AppSettingsContainer = styled('div')`
    width: 100%;
    height: 100%;
    .anti-spy-actions{
        display: flex;
        gap: 12px;
    }
`